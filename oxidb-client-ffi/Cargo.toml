[package]
name = "oxidb-client-ffi"
version = "0.25.0"
edition = "2024"
description = "C-compatible FFI client library for OxiDB server"
license = "MIT OR Apache-2.0"
repository = "https://github.com/parisxmas/OxiDB"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde_json = "1"
