[package]
name = "oxidb-server"
version = "0.1.0"
edition = "2024"
description = "TCP server for OxiDB document database"

[dependencies]
oxidb = { path = ".." }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
base64 = "0.22"
