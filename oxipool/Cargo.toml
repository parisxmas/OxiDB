[package]
name = "oxipool"
version = "0.1.0"
edition = "2024"
description = "Connection pooler for OxiDB — like PgBouncer for OxiDB"

[dependencies]
tokio = { version = "1", features = ["rt-multi-thread", "net", "io-util", "sync", "time", "macros", "signal"] }
