[package]
name = "oxidb-rec"
version = "0.1.0"
edition = "2024"
description = "OxiDB recommendation engine — real-time item-to-item co-occurrence counters with LLR scoring (ADR-0025)."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
